@model IEnumerable<InteractHealthProDatabase.Models.Case>

@{
    ViewData["Title"] = "Index";
}

<h2>Case Index</h2>

<p>
    <a asp-action="Create">Create a New Case</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CaseRef)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OtherRef)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CaseRef)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OtherRef)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
